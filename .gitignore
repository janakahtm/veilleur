# Secrets - ne jamais committer
.env

# Dépendances Node (volumineux, réinstallable)
node_modules/

#Briefing generes ( resultats, pas du code source)
briefing-*.txt

# Journal d'exécution (résultat, pas du code)
cron.log